Real time image processing to detect objects in video. Start with picking up straight edges in still images, then in video. This would be even cooler if it could run on a phone.

Rogue-like 'Hero' based RPG, to run at terminal. Ncurses?
Some sort of time logging system but based on 'real life' tasks, and learns about what you use to achieve those real goals using applications. Capable of producing a work log which makes more sense than simply which apps you had open.

Super simple vst host
Control vst from command line with midi sends
midi band in a script
